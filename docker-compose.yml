services:
  app:
    build: .
    container_name: ai-interview-copilot
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: ai-interview-redis
    ports:
      - "6379:6379"
